// const sendUserInfo = require("../Server/Controllers/AudioController");

// test("lat to be a number greater than 1", (req, res) => {
//   const lat = req.body;
//   expect(sendUserInfo(req)).toBeGreaterThan(1);
// });

// test("lng to be a number greater than 1", req => {
//   const lng = req.body;
//   expect(lng).toBeGreaterThan(1);
// });

// test();
// test();
// test();
